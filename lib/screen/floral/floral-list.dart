import 'package:flutter/material.dart';

class FloralList extends StatelessWidget {
  const FloralList({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(

    );
  }
}